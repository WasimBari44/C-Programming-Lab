#include <stdio.h>
int main()
{
    int first_number=50;
    int second_number=60;
    int total_sum;

    total_sum=first_number+second_number;

    printf("Total Sum =%d",total_sum);

    return 0;
}
